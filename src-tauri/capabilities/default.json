{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window: core APIs (invoke + events) + native dialog (folder picker).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
