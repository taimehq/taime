/** Placeholder — the Screens phase replaces this with the fleet overview. */
export function DashboardScreen() {
  return (
    <div className="flex h-full flex-col gap-1 p-6">
      <h1 className="text-sm font-medium text-zinc-100">Dashboard</h1>
      <p className="text-xs text-zinc-500">
        Fleet overview lands here. Tasks and agents are in the sidebar; ⌘K
        jumps anywhere.
      </p>
    </div>
  );
}
